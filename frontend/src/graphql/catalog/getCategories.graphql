query GetCategories {
  offersModule {
    rootCategories {
      id
      name
      slug
      parentId
      isLeaf
      children {
        id
        name
        slug
        parentId
        isLeaf
      }
    }
  }
}
