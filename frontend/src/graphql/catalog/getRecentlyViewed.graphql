query RecentlyViewed($userId: String, $sessionId: String, $limit: Int) {
  offersModule {
    recentlyViewedProducts(userId: $userId, sessionId: $sessionId, limit: $limit) {
      id
      slug
      name
      mainImageUrl
      priceFrom {
        amount
        currency
      }
    }
  }
}
