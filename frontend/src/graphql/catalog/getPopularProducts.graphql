query PopularProducts($limit: Int) {
  offersModule {
    popularProducts(limit: $limit) {
      id
      slug
      name
      mainImageUrl
      priceFrom {
        amount
        currency
      }
    }
  }
}
