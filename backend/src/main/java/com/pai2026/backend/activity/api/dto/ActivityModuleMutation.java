package com.pai2026.backend.activity.api.dto;

/**
 * Namespace placeholder for {@code Mutation.activityModule}. Carries no data;
 * its fields (e.g. {@code recordActivityEvent}) are resolved via
 * {@code @SchemaMapping(typeName = "ActivityModuleMutation")}.
 */
public record ActivityModuleMutation() {
}
