package com.pai2026.backend.activity.api.dto;

/**
 * Namespace placeholder for {@code Query.activityModule}. Carries no data; its
 * fields (summary, topProducts, …) are resolved via
 * {@code @SchemaMapping(typeName = "ActivityModuleQuery")}.
 */
public record ActivityModuleQuery() {
}
